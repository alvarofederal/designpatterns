package arquiteturas.designPatterns.factoryMethod;

//Vamos começar com uma interface que define o contrato para o produto que será criado pelo Factory Method.
interface Product {
	void doSomething();
}
