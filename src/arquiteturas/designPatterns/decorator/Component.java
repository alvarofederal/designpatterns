package arquiteturas.designPatterns.decorator;

//Vamos começar com a interface Component, que define o contrato para o componente base e todos os seus decoradores.
interface Component {
 void operation();
}
